version: 2

models:
  - name: customers
    description: '{{ doc("customers") }}'
    columns:
      - name: customer_id
        description: Primary Key
        tests:
          - unique
          - not_null