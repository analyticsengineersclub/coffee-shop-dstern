version: 2

sources:
  - name: coffee_shop
    description: This is snapshot of our OLTP data, not updated.
    project: analytics-engineers-club
    tables:
      - name: customers
        columns: 
          - name: id
            tests:
              - unique
              - not_null

      - name: orders
        columns:
          - name: id
            tests: 
              - unique
              - not_null